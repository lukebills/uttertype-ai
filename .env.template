# UtterType AI Configuration
# Copy this file to .env and configure your settings

# ================================
# REQUIRED CONFIGURATION
# ================================

# OpenAI API Key (Required)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# ================================
# OPTIONAL CONFIGURATION
# ================================

# OpenAI API Configuration
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_MODEL_NAME=whisper-1

# Hotkey Configuration
# Use format: <modifier>+<key> (e.g., <ctrl>+<alt>+q)
UTTERTYPE_RECORD_HOTKEYS=<ctrl>+<alt>+q
UTTERTYPE_AI_HOTKEYS=<ctrl>+<alt>+a
UTTERTYPE_EMAIL_HOTKEY=<ctrl>+<alt>+<shift>+e

# Audio Configuration
UTTERTYPE_MIN_TRANSCRIPTION_SIZE_MS=1500
# UTTERTYPE_AUDIO_DEVICE_INDEX=0  # Uncomment to specify audio device

# AI Configuration
UTTERTYPE_AI_TEMPERATURE=0.3
UTTERTYPE_EMAIL_TEMPERATURE=0.2
UTTERTYPE_MAX_TOKENS=1000

# Application Configuration
UTTERTYPE_LOG_LEVEL=INFO
# UTTERTYPE_VAD_ENABLED=true  # Uncomment to enable Voice Activity Detection

# Advanced Configuration (rarely needed)
UTTERTYPE_DEBOUNCE_TIME_SEC=2.0
UTTERTYPE_MAX_DISPLAY_TEXT_LENGTH=50
UTTERTYPE_CONSOLE_REFRESH_DELAY=0.1

# ================================
# CONFIGURATION NOTES
# ================================

# Temperature: Controls AI creativity (0.0-2.0)
#   - 0.0: Very deterministic
#   - 1.0: Balanced creativity
#   - 2.0: Very creative (may be inconsistent)

# Log Levels: DEBUG, INFO, WARNING, ERROR, CRITICAL

# Hotkey Modifiers: <ctrl>, <alt>, <shift>, <cmd> (macOS)
# Hotkey Keys: Letters (a-z), numbers (0-9), function keys (f1-f12)

# Audio Device Index: Run list_audio_devices.py to see available devices
